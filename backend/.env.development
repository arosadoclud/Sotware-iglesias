# ═══════════════════════════════════════════════════════════
#  Church Program Manager – Variables de Entorno DESARROLLO
#  Configuración segura para trabajar en local
# ═══════════════════════════════════════════════════════════

# ── SERVIDOR ─────────────────────────────────────────────────
NODE_ENV=development
PORT=5000
API_VERSION=v1
FRONTEND_URL=http://localhost:5173

# URL base del API (para generar enlaces públicos de PDFs)
API_BASE_URL=http://localhost:5000

# ── BASE DE DATOS ────────────────────────────────────────────
# MongoDB local para desarrollo
MONGODB_URI=mongodb://localhost:27017/church-program-manager-dev

# ── JWT ──────────────────────────────────────────────────────
# IMPORTANTE: Estos son secretos DE DESARROLLO
# En producción usa secretos diferentes y seguros
JWT_SECRET=cambia_esto_por_un_secreto_muy_seguro_min_32_chars_desarrollo
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=otro_secreto_diferente_para_el_refresh_token_desarrollo
JWT_REFRESH_EXPIRES_IN=7d

# ── CLOUDINARY (para logos de iglesias) ─────────────────────
# Dejar vacío en desarrollo o usar cuenta de prueba
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# ── REDIS (opcional – cola de notificaciones + cache) ───────
# Si no se configura, el sistema funciona en modo síncrono
# Comentar si no tienes Redis instalado localmente
# REDIS_URL=redis://localhost:6379

# ── EMAIL ────────────────────────────────────────────────────
# Usar mailtrap para desarrollo (no envía emails reales)
EMAIL_PROVIDER=smtp
EMAIL_FROM=noreply@desarrollo.local
EMAIL_FROM_NAME=Iglesia Desarrollo

# SMTP genérico (Gmail para pruebas locales)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

# Mailtrap (recomendado para desarrollo)
# Regístrate en mailtrap.io y obtén las credenciales
# MAILTRAP_USER=
# MAILTRAP_PASS=

# ── WHATSAPP ─────────────────────────────────────────────────
# Deshabilitado en desarrollo
WHATSAPP_PROVIDER=none

# ── PUPPETEER (PDF) ──────────────────────────────────────────
# Dejar vacío para usar el Chromium instalado por puppeteer
PUPPETEER_EXECUTABLE_PATH=

# ── RATE LIMITING ────────────────────────────────────────────
# Más permisivo en desarrollo
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=200

# ── LÓGICA DE NEGOCIO ────────────────────────────────────────
DEFAULT_ROTATION_WEEKS=4
MAX_FILE_SIZE=5242880
DEFAULT_TIMEZONE=America/Santo_Domingo

# ── LOGGING ──────────────────────────────────────────────────
LOG_LEVEL=debug
