# ═══════════════════════════════════════════════════════════
#  Church Program Manager — Variables de Entorno
#  Copia a .env y rellena los valores
# ═══════════════════════════════════════════════════════════

# ── SERVIDOR ──────────────────────────────────────────────
NODE_ENV=development
PORT=5000
API_VERSION=v1
FRONTEND_URL=http://localhost:5173

# ── BASE DE DATOS ─────────────────────────────────────────
MONGODB_URI=mongodb://localhost:27017/church-program-manager

# ── JWT ───────────────────────────────────────────────────
JWT_SECRET=cambia_esto_por_un_secreto_muy_seguro_min_32_chars
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=otro_secreto_diferente_para_el_refresh_token
JWT_REFRESH_EXPIRES_IN=7d

# ── CLOUDINARY (para logos de iglesias) ──────────────────
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# ── REDIS (opcional — cola de notificaciones + cache) ─────
# Si no se configura, el sistema funciona en modo síncrono
# REDIS_URL=redis://localhost:6379

# ── EMAIL ─────────────────────────────────────────────────
# Proveedores: sendgrid | smtp | mailtrap
EMAIL_PROVIDER=smtp
EMAIL_FROM=noreply@miglesia.com
EMAIL_FROM_NAME=Mi Iglesia

# SendGrid
# SENDGRID_API_KEY=SG...

# SMTP genérico
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

# Mailtrap (desarrollo)
# MAILTRAP_USER=
# MAILTRAP_PASS=

# ── WHATSAPP ──────────────────────────────────────────────
# Proveedores: twilio | meta | none
WHATSAPP_PROVIDER=none

# Twilio
# TWILIO_ACCOUNT_SID=AC...
# TWILIO_AUTH_TOKEN=...
# TWILIO_WHATSAPP_FROM=whatsapp:+14155238886

# Meta WhatsApp Cloud API
# META_WABA_TOKEN=...
# META_PHONE_NUMBER_ID=...

# ── PUPPETEER (PDF) ────────────────────────────────────────
# Dejar vacío para usar el Chromium instalado por puppeteer
# En servidores Linux sin GUI: /usr/bin/chromium-browser
PUPPETEER_EXECUTABLE_PATH=

# ── RATE LIMITING ─────────────────────────────────────────
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=200

# ── LÓGICA DE NEGOCIO ─────────────────────────────────────
DEFAULT_ROTATION_WEEKS=4
MAX_FILE_SIZE=5242880
DEFAULT_TIMEZONE=America/Santo_Domingo

# ── LOGGING ───────────────────────────────────────────────
LOG_LEVEL=info
