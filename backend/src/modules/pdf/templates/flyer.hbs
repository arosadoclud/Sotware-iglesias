<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>{{churchName}} - {{worshipType}}</title>
<style>
  @import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,600;0,700;1,400&family=DM+Sans:wght@300;400;500;600&display=swap');

  * { box-sizing: border-box; margin: 0; padding: 0; }

  @page {
    size: Letter;
    margin: 0;
  }

  body {
    font-family: 'DM Sans', sans-serif;
    background: white;
    color: #111827;
    width: 8.5in;
    height: 11in;
    margin: 0;
    padding: 0;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .flyer-container {
    background: white;
    width: 7in;
    min-height: 9.5in;
    display: flex;
    flex-direction: column;
    border: 1px solid #E5E7EB;
    border-radius: 16px;
    overflow: hidden;
    box-shadow: 0 4px 24px rgba(0,0,0,0.06);
  }

  /* ── Header ── */
  .flyer-header {
    background: linear-gradient(135deg, #1B2D5B 0%, #2A4080 60%, #1B2D5B 100%);
    padding: 2.8rem 2.5rem 2rem;
    position: relative;
    overflow: hidden;
  }

  .flyer-header-circle1 {
    position: absolute;
    top: -50px;
    right: -50px;
    width: 200px;
    height: 200px;
    background: rgba(200,168,75,0.08);
    border-radius: 50%;
  }

  .flyer-header-circle2 {
    position: absolute;
    bottom: -30px;
    left: -40px;
    width: 150px;
    height: 150px;
    background: rgba(200,168,75,0.05);
    border-radius: 50%;
  }

  .header-inner {
    display: flex;
    align-items: center;
    gap: 1.5rem;
    position: relative;
    z-index: 1;
  }

  .header-text {
    flex: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .flyer-church-name {
    font-family: 'Cormorant Garamond', serif;
    font-size: 1.85rem;
    font-weight: 700;
    color: white;
    line-height: 1.15;
    letter-spacing: 0.02em;
    text-align: center;
    margin-bottom: 6px;
  }

  .flyer-church-sub {
    font-size: 0.9rem;
    color: #E8C96A;
    margin-top: 4px;
    letter-spacing: 0.05em;
    text-align: center;
  }

  .flyer-church-loc {
    font-size: 0.8rem;
    color: rgba(255,255,255,0.5);
    margin-top: 4px;
    text-align: center;
  }

  .logo-wrap {
    flex-shrink: 0;
    width: 80px;
    height: 80px;
  }

  .logo-wrap img {
    width: 80px;
    height: 80px;
    border-radius: 12px;
    object-fit: contain;
    border: 2px solid rgba(200,168,75,0.4);
    background: rgba(255,255,255,0.1);
  }

  .logo-placeholder {
    width: 80px;
    height: 80px;
    background: rgba(255,255,255,0.1);
    border: 2px solid rgba(200,168,75,0.5);
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 2rem;
    color: rgba(255,255,255,0.6);
  }

  /* ── Gold Band ── */
  .gold-band {
    height: 5px;
    background: linear-gradient(90deg, #C8A84B 0%, #E8C96A 50%, #C8A84B 100%);
  }

  /* ── Badge ── */
  .flyer-badge-row {
    display: flex;
    justify-content: center;
    padding: 1.5rem 2.5rem 0.6rem;
  }

  .flyer-badge {
    background: #C8A84B;
    color: #1B2D5B;
    font-family: 'Cormorant Garamond', serif;
    font-size: 1.35rem;
    font-weight: 700;
    padding: 8px 36px;
    border-radius: 30px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  /* ── Date & Time ── */
  .flyer-date-row {
    text-align: center;
    padding: 0.6rem 2.5rem 0.4rem;
  }

  .flyer-date {
    font-family: 'Cormorant Garamond', serif;
    font-size: 1.35rem;
    color: #1B2D5B;
    font-weight: 600;
  }

  .flyer-time {
    font-size: 1rem;
    color: #6B7280;
    margin-top: 4px;
  }

  /* ── Ornament ── */
  .ornament {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 0.8rem 2.5rem;
  }

  .ornament-line {
    flex: 1;
    height: 1px;
    background: linear-gradient(90deg, transparent, #D1D5DB, transparent);
  }

  .ornament-diamonds {
    display: flex;
    gap: 6px;
  }

  .ornament-diamonds span {
    display: block;
    width: 6px;
    height: 6px;
    background: #C8A84B;
    transform: rotate(45deg);
    opacity: 0.7;
  }

  .ornament-diamonds span:nth-child(2) {
    opacity: 1;
  }

  /* ── Section Title ── */
  .flyer-section-title {
    text-align: center;
    font-size: 0.8rem;
    font-weight: 700;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: #1B2D5B;
    padding-bottom: 0.8rem;
  }

  /* ── Table ── */
  .flyer-table {
    padding: 0 2rem 1.5rem;
    flex: 1;
  }

  .flyer-row {
    display: flex;
    align-items: center;
    padding: 12px 16px;
    border-radius: 10px;
    margin-bottom: 6px;
    min-height: 52px;
  }

  .flyer-row:nth-child(odd) {
    background: #F3F4F6;
  }

  .flyer-row:nth-child(even) {
    background: white;
    border: 1px solid #F3F4F6;
  }

  .flyer-row-num {
    flex-shrink: 0;
    width: 28px;
    height: 28px;
    background: #1B2D5B;
    color: #E8C96A;
    border-radius: 6px;
    font-size: 0.8rem;
    font-weight: 700;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-right: 14px;
  }

  .flyer-row-role {
    flex: 0 0 auto;
    width: 180px;
    font-size: 0.95rem;
    font-weight: 600;
    color: #1B2D5B;
  }

  .flyer-row-person {
    flex: 1;
    font-family: 'Cormorant Garamond', serif;
    font-size: 1.15rem;
    font-weight: 600;
    color: #111827;
    font-style: italic;
  }

  .flyer-row-person.empty {
    color: #D1D5DB;
    font-style: normal;
    font-family: 'DM Sans', sans-serif;
    font-size: 0.85rem;
    font-weight: 400;
    letter-spacing: 0.03em;
  }

  /* ── Verse ── */
  .flyer-verse {
    text-align: center;
    padding: 1rem 2.5rem 0.8rem;
    border-top: 1px solid #F3F4F6;
    margin: 0 2rem;
  }

  .flyer-verse p {
    font-family: 'Cormorant Garamond', serif;
    font-size: 1.05rem;
    font-style: italic;
    color: #6B7280;
    line-height: 1.5;
    margin: 0;
  }

  /* ── Summary ── */
  .flyer-summary {
    text-align: center;
    padding: 0.5rem 2.5rem 0.5rem;
    margin: 0 2rem;
  }

  .flyer-summary p {
    font-size: 0.85rem;
    color: #6B7280;
    line-height: 1.4;
    margin: 0;
  }

  /* ── Footer ── */
  .flyer-footer {
    background: #1B2D5B;
    padding: 14px 2.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-top: auto;
  }

  .flyer-footer-text {
    font-size: 0.85rem;
    font-weight: 600;
    color: rgba(255,255,255,0.7);
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }
</style>
</head>
<body>
<div class="flyer-container">

  <!-- Header -->
  <div class="flyer-header">
    <div class="flyer-header-circle1"></div>
    <div class="flyer-header-circle2"></div>

    <div class="header-inner">
      <div class="header-text">
        <div class="flyer-church-name">{{churchName}}</div>
        {{#if subtitle}}
        <div class="flyer-church-sub">{{subtitle}}</div>
        {{/if}}
        {{#if location}}
        <div class="flyer-church-loc">{{location}}</div>
        {{/if}}
      </div>
      <div class="logo-wrap">
        {{#if logoUrl}}
        <img src="{{logoUrl}}" alt="Logo">
        {{else}}
        <div class="logo-placeholder">&#10013;</div>
        {{/if}}
      </div>
    </div>
  </div>

  <div class="gold-band"></div>

  <!-- Badge -->
  <div class="flyer-badge-row">
    <div class="flyer-badge">{{worshipType}}</div>
  </div>

  <!-- Date & Time -->
  <div class="flyer-date-row">
    <div class="flyer-date">{{formattedDate}}</div>
    {{#if formattedTime}}
    <div class="flyer-time">{{formattedTime}}</div>
    {{/if}}
  </div>

  <!-- Ornament -->
  <div class="ornament">
    <div class="ornament-line"></div>
    <div class="ornament-diamonds">
      <span></span><span></span><span></span>
    </div>
    <div class="ornament-line"></div>
  </div>

  <div class="flyer-section-title">Orden del Culto</div>

  <!-- Assignments Table -->
  <div class="flyer-table">
    {{#each assignments}}
    <div class="flyer-row">
      <div class="flyer-row-num">{{padId id}}</div>
      <div class="flyer-row-role">{{roleName}}</div>
      <div class="flyer-row-person{{#unless personName}} empty{{/unless}}">
        {{#if personName}}{{personName}}{{else}}&mdash; &mdash; &mdash; &mdash; &mdash; &mdash; &mdash;{{/if}}
      </div>
    </div>
    {{/each}}
  </div>

  {{#if verse}}
  <div class="flyer-verse">
    <p>{{verse}}</p>
  </div>
  {{/if}}

  {{#if summary}}
  <div class="flyer-summary">
    <p>{{summary}}</p>
  </div>
  {{/if}}

  <!-- Footer -->
  <div class="flyer-footer">
    <span class="flyer-footer-text">{{churchNameUpper}}</span>
  </div>

</div>
</body>
</html>
