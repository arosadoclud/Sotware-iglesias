<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>{{churchName}} - {{worshipType}}</title>
<style>
  @import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,600;0,700;1,400&family=Playfair+Display:wght@700;900&family=Inter:wght@300;400;500;600;700&display=swap');
  
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  body {
    font-family: 'Inter', -apple-system, system-ui, sans-serif;
    background: #F9FAFB;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 2rem 1rem;
  }

  .flyer-container {
    background: white;
    border-radius: 20px;
    box-shadow: 0 20px 50px rgba(27, 45, 91, 0.15);
    overflow: hidden;
    width: 100%;
    max-width: 600px;
  }

  /* Header */
  .flyer-header {
    background: linear-gradient(135deg, #1B2D5B 0%, #2A4080 50%, #1B2D5B 100%);
    padding: 2.5rem 2.5rem 2rem;
    position: relative;
    overflow: hidden;
  }

  .flyer-header::before {
    content: '';
    position: absolute;
    top: -50%;
    right: -20%;
    width: 300px;
    height: 300px;
    background: radial-gradient(circle, rgba(212, 175, 55, 0.15) 0%, transparent 70%);
    border-radius: 50%;
  }

  .header-inner {
    display: flex;
    align-items: center;
    gap: 1.5rem;
    position: relative;
    z-index: 1;
  }

  .logo-circle {
    width: 80px;
    height: 80px;
    background: rgba(255, 255, 255, 0.1);
    border: 3px solid rgba(212, 175, 55, 0.4);
    border-radius: 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    backdrop-filter: blur(10px);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.2);
    font-size: 2rem;
    color: rgba(212, 175, 55, 0.8);
  }

  .logo-circle img {
    width: 65px;
    height: 65px;
    object-fit: contain;
  }

  .header-text {
    flex: 1;
  }

  .flyer-church-name {
    font-family: 'Playfair Display', serif;
    font-size: 1.75rem;
    font-weight: 900;
    color: white;
    line-height: 1.1;
    letter-spacing: -0.02em;
    text-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
  }

  .flyer-church-sub {
    font-size: 0.875rem;
    color: #F4D03F;
    margin-top: 0.5rem;
    letter-spacing: 0.1em;
    font-weight: 500;
  }

  .flyer-church-loc {
    font-size: 0.75rem;
    color: rgba(255, 255, 255, 0.6);
    margin-top: 0.25rem;
    font-weight: 400;
  }

  /* Banda dorada */
  .gold-band {
    height: 5px;
    background: linear-gradient(90deg, 
      transparent 0%, 
      #D4AF37 10%, 
      #F4D03F 50%, 
      #D4AF37 90%, 
      transparent 100%
    );
  }

  /* Badge */
  .flyer-badge-row {
    display: flex;
    justify-content: center;
    padding: 2rem 2.5rem 1rem;
  }

  .flyer-badge {
    background: linear-gradient(135deg, #D4AF37 0%, #F4D03F 100%);
    color: #1B2D5B;
    font-family: 'Playfair Display', serif;
    font-size: 1.25rem;
    font-weight: 700;
    padding: 0.75rem 2.5rem;
    border-radius: 50px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    box-shadow: 0 4px 16px rgba(212, 175, 55, 0.3);
  }

  /* Fecha y hora */
  .flyer-date-row {
    text-align: center;
    padding: 1rem 2.5rem 0.5rem;
  }

  .flyer-date {
    font-family: 'Playfair Display', serif;
    font-size: 1.5rem;
    color: #1B2D5B;
    font-weight: 700;
    letter-spacing: -0.01em;
  }

  .flyer-time {
    font-size: 1rem;
    color: #6B7280;
    margin-top: 0.5rem;
    font-weight: 500;
  }

  /* Ornamento */
  .ornament {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1.5rem 2.5rem;
  }

  .ornament-line {
    flex: 1;
    height: 1px;
    background: linear-gradient(90deg, transparent, #D1D5DB, transparent);
  }

  .ornament-diamonds {
    display: flex;
    gap: 6px;
  }

  .ornament-diamonds span {
    display: block;
    width: 6px;
    height: 6px;
    background: #D4AF37;
    transform: rotate(45deg);
    opacity: 0.5;
  }

  .ornament-diamonds span:nth-child(2) {
    opacity: 1;
    transform: rotate(45deg) scale(1.3);
  }

  /* Título */
  .flyer-section-title {
    text-align: center;
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: #1B2D5B;
    padding-bottom: 1rem;
    position: relative;
  }

  .flyer-section-title::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 60px;
    height: 2px;
    background: #D4AF37;
  }

  /* Tabla */
  .flyer-table {
    padding: 0 2rem 1.5rem;
  }

  .flyer-row {
    display: flex;
    align-items: center;
    padding: 1rem 1.25rem;
    border-radius: 12px;
    margin-bottom: 0.5rem;
    min-height: 52px;
    border: 2px solid transparent;
  }

  .flyer-row:nth-child(odd) {
    background: linear-gradient(135deg, #F9FAFB 0%, #F3F4F6 100%);
  }

  .flyer-row:nth-child(even) {
    background: white;
    border-color: #F3F4F6;
  }

  .flyer-row-num {
    flex-shrink: 0;
    width: 32px;
    height: 32px;
    background: linear-gradient(135deg, #1B2D5B 0%, #2A4080 100%);
    color: #F4D03F;
    border-radius: 8px;
    font-size: 0.875rem;
    font-weight: 700;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-right: 1rem;
    box-shadow: 0 2px 8px rgba(27, 45, 91, 0.2);
  }

  .flyer-row-role {
    flex-shrink: 0;
    width: 160px;
    font-size: 0.9rem;
    font-weight: 600;
    color: #1B2D5B;
  }

  .flyer-row-person {
    flex: 1;
    font-family: 'Cormorant Garamond', serif;
    font-size: 1.125rem;
    font-weight: 600;
    color: #1F2937;
    font-style: italic;
    text-align: right;
  }

  .flyer-row-person.empty {
    color: #D1D5DB;
    font-style: normal;
    font-family: 'Inter', sans-serif;
    font-size: 0.875rem;
    font-weight: 400;
  }

  /* Versículo */
  .flyer-verse {
    text-align: center;
    padding: 1.5rem 2.5rem;
    border-top: 2px solid #F3F4F6;
    margin: 0.5rem 2rem 0;
    background: linear-gradient(135deg, #FFF9E6 0%, rgba(255, 249, 230, 0.3) 100%);
    border-radius: 12px;
  }

  .flyer-verse p {
    font-family: 'Cormorant Garamond', serif;
    font-size: 1rem;
    font-style: italic;
    color: #374151;
    line-height: 1.6;
    margin-bottom: 0.5rem;
  }

  .flyer-verse cite {
    font-size: 0.8rem;
    font-weight: 700;
    color: #D4AF37;
    font-style: normal;
    letter-spacing: 0.08em;
    display: block;
  }

  /* Footer */
  .flyer-footer {
    background: linear-gradient(135deg, #1B2D5B 0%, #2A4080 100%);
    padding: 1.25rem 2.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-top: 1.5rem;
    position: relative;
  }

  .flyer-footer::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 2px;
    background: linear-gradient(90deg, transparent, #D4AF37, transparent);
  }

  .flyer-footer-text {
    font-size: 0.875rem;
    font-weight: 600;
    color: rgba(255, 255, 255, 0.9);
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }
</style>
</head>
<body>
<div class="flyer-container">
  <div class="flyer-header">
    <div class="header-inner">
      <div class="logo-circle">
        {{#if logoUrl}}
        <img src="{{logoUrl}}" alt="Logo">
        {{else}}
        ✝
        {{/if}}
      </div>
      <div class="header-text">
        <div class="flyer-church-name">{{churchName}}</div>
        {{#if subtitle}}
        <div class="flyer-church-sub">{{subtitle}}</div>
        {{/if}}
        {{#if location}}
        <div class="flyer-church-loc">{{location}}</div>
        {{/if}}
      </div>
    </div>
  </div>

  <div class="gold-band"></div>

  <div class="flyer-badge-row">
    <div class="flyer-badge">{{worshipType}}</div>
  </div>

  <div class="flyer-date-row">
    <div class="flyer-date">{{formattedDate}}</div>
    <div class="flyer-time">{{programTime}}</div>
  </div>

  <div class="ornament">
    <div class="ornament-line"></div>
    <div class="ornament-diamonds">
      <span></span><span></span><span></span>
    </div>
    <div class="ornament-line"></div>
  </div>

  <div class="flyer-section-title">Orden del Culto</div>

  <div class="flyer-table">
    {{#each assignments}}
    <div class="flyer-row">
      <div class="flyer-row-num">{{padId id}}</div>
      <div class="flyer-row-role">{{roleName}}</div>
      <div class="flyer-row-person{{#unless personName}} empty{{/unless}}">
        {{#if personName}}{{personName}}{{else}}Sin asignar{{/if}}
      </div>
    </div>
    {{/each}}
  </div>

  {{#if verseText}}
  <div class="flyer-verse">
    <p>"{{verseText}}"</p>
    {{#if verseRef}}
    <cite>{{verseRef}}</cite>
    {{/if}}
  </div>
  {{/if}}

  <div class="flyer-footer">
    <span class="flyer-footer-text">{{churchName}}</span>
  </div>
</div>
</body>
</html>